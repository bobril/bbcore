let getGlobals = () => globals, setGlobals = a => {
    globals = {
        isSortingDisabled: a
    };
}, globals = {
    isSortingDisabled: !1
};

function getDescriptor() {
    return {
        asd: "df"
    };
}

export { getGlobals, setGlobals, getDescriptor };

