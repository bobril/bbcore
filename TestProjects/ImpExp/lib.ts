import { default as EE } from "./deeplib";

export { EE };

import * as FormElements from "./form";

const touchPointLabelWidth = FormElements.LabelWidth.Label200;

export const commonDetailLabelStyle = console.log({
    width: touchPointLabelWidth,
    cssFloat: "left",
});
